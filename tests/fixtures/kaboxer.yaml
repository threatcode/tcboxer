application:
    id: CONTAINERID
    name: Alpine
    description: >
        test container for kaboxer
    categories: Utilities;Development
container:
  type: docker
build:
  docker:
    file: Dockerfile
components:
  default:
    run_mode: cli
    executable: /bin/sh
